{
    "devCommand": "python3 -m uvicorn webapp.main:app --host 0.0.0.0 --port 8000 --reload",
    "builds": [
      {
        "src": ".",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "webapp/main.py"
      }
    ]
}
